import { getSiteContent } from "@/lib/site-content";

const defaults = { heading: "Made by us.", subheading: "Crafted for you.", description: "We believe the best comfort comes from staying close to the making. From selecting a breathable fabric to the final fold, our own manufacturing team is present at every step." };
export default async function ManufacturingPage() { const content = await getSiteContent("our-making", defaults); return <div><div className="mx-auto max-w-[1100px] px-6 py-16 md:px-12 md:py-24"><p className="eyebrow text-rose">Inside SHARKI</p><h1 className="display mt-4 max-w-3xl text-7xl leading-[.88]">{content.heading}<br />{content.subheading}</h1><p className="mt-8 max-w-xl text-sm leading-7 text-ink/65">{content.description}</p><div className="mt-20 grid gap-4 sm:grid-cols-2">{[["01", "Fabric selection", "We begin with fabrics that feel good against the skin and hold their shape over time."], ["02", "Cutting", "Each panel is cut with care for a balanced, relaxed silhouette."], ["03", "Stitching", "Experienced hands bring together neat seams and considered finishing."], ["04", "Quality check", "Every piece is checked, pressed and packed before it leaves our house."]].map(([n, title, text]) => <div key={n} className="min-h-[240px] bg-sand p-7"><span className="text-xs text-rose">{n}</span><h2 className="display mt-12 text-4xl">{title}</h2><p className="mt-3 max-w-sm text-sm leading-6 text-ink/60">{text}</p></div>)}</div></div></div>; }
