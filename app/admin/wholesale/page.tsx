import { db } from "@/lib/db";
import { requireAdmin } from "@/lib/admin-auth";

export default async function WholesaleAdminPage() { await requireAdmin(); let enquiries: { id: string; businessName: string; contactName: string; email: string; status: string; createdAt: Date }[] = []; try { enquiries = await db.wholesaleEnquiry.findMany({ orderBy: { createdAt: "desc" } }); } catch { /* Empty state when the configured database is unavailable. */ } return <div><p className="text-xs font-semibold uppercase tracking-[.14em] text-[#71808c]">B2B</p><h1 className="mt-2 text-3xl font-semibold">Wholesale enquiries</h1><div className="mt-8 overflow-x-auto rounded-lg border border-[#e2e7eb] bg-white"><table className="w-full min-w-[700px] text-left text-sm"><thead className="border-b border-[#e2e7eb] bg-[#f8fafb] text-xs uppercase tracking-wide text-[#71808c]"><tr><th className="p-4">Business</th><th>Contact</th><th>Email</th><th>Status</th><th>Date</th></tr></thead><tbody>{enquiries.map((enquiry) => <tr key={enquiry.id} className="border-b border-[#eef1f3]"><td className="p-4 font-medium">{enquiry.businessName}</td><td>{enquiry.contactName}</td><td>{enquiry.email}</td><td>{enquiry.status}</td><td>{enquiry.createdAt.toLocaleDateString("en-IN")}</td></tr>)}{!enquiries.length && <tr><td colSpan={5} className="p-8 text-center text-[#71808c]">No wholesale enquiries in the database.</td></tr>}</tbody></table></div></div>; }
