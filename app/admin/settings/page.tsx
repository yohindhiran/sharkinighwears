"use client";

import { FormEvent, useEffect, useState } from "react";

const defaults = { storeName: "SHARKI NIGHTWEARS", email: "", phone: "", address: "", currency: "INR", shippingInfo: "", seoDescription: "" };
export default function SettingsPage() { const [form, setForm] = useState(defaults); const [message, setMessage] = useState(""); useEffect(() => { fetch("/api/admin/content/settings").then((response) => response.json()).then((data) => setForm({ ...defaults, ...data })); }, []); async function submit(event: FormEvent) { event.preventDefault(); const response = await fetch("/api/admin/content/settings", { method: "PUT", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) }); setMessage(response.ok ? "Settings saved." : "Unable to save settings."); } return <div><p className="text-xs font-semibold uppercase tracking-[.14em] text-[#71808c]">Workspace</p><h1 className="mt-2 text-3xl font-semibold">Settings</h1><form onSubmit={submit} className="mt-8 grid max-w-3xl gap-5 rounded-lg border border-[#e2e7eb] bg-white p-6 md:grid-cols-2"><input value={form.storeName} onChange={(event) => setForm({ ...form, storeName: event.target.value })} placeholder="Store name" className="admin-input" /><input value={form.currency} onChange={(event) => setForm({ ...form, currency: event.target.value })} placeholder="Currency" className="admin-input" /><input type="email" value={form.email} onChange={(event) => setForm({ ...form, email: event.target.value })} placeholder="Store email" className="admin-input" /><input value={form.phone} onChange={(event) => setForm({ ...form, phone: event.target.value })} placeholder="Store phone" className="admin-input" /><textarea value={form.address} onChange={(event) => setForm({ ...form, address: event.target.value })} placeholder="Store address" className="admin-input md:col-span-2" /><textarea value={form.shippingInfo} onChange={(event) => setForm({ ...form, shippingInfo: event.target.value })} placeholder="Shipping information" className="admin-input md:col-span-2" /><textarea value={form.seoDescription} onChange={(event) => setForm({ ...form, seoDescription: event.target.value })} placeholder="SEO default description" className="admin-input md:col-span-2" /><button className="w-fit rounded-md bg-[#2563eb] px-4 py-2.5 text-sm font-medium text-white">Save settings</button>{message && <p className="self-center text-sm text-[#287a3d]">{message}</p>}</form></div>; }
