"use client";

import { FormEvent, useEffect, useState } from "react";
const defaults = { heading: "Made by us.", subheading: "Crafted for you.", description: "We believe the best comfort comes from staying close to the making. From selecting a breathable fabric to the final fold, our own manufacturing team is present at every step." };
export default function AdminOurMakingPage() { const [form, setForm] = useState(defaults); const [message, setMessage] = useState(""); useEffect(() => { fetch("/api/admin/content/our-making").then((response) => response.json()).then((data) => setForm({ ...defaults, ...data })); }, []); async function submit(event: FormEvent) { event.preventDefault(); const response = await fetch("/api/admin/content/our-making", { method: "PUT", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) }); setMessage(response.ok ? "Our Making content saved." : "Unable to save content."); } return <div><p className="text-xs font-semibold uppercase tracking-[.14em] text-[#71808c]">Website content</p><h1 className="mt-2 text-3xl font-semibold">Our Making</h1><form onSubmit={submit} className="mt-8 max-w-3xl space-y-5 rounded-lg border border-[#e2e7eb] bg-white p-6"><input value={form.heading} onChange={(event) => setForm({ ...form, heading: event.target.value })} placeholder="Heading" className="admin-input" /><input value={form.subheading} onChange={(event) => setForm({ ...form, subheading: event.target.value })} placeholder="Subheading" className="admin-input" /><textarea value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} rows={5} placeholder="Description" className="admin-input" /><button className="rounded-md bg-[#2563eb] px-4 py-2.5 text-sm font-medium text-white">Save content</button>{message && <p className="text-sm text-[#287a3d]">{message}</p>}</form></div>; }
