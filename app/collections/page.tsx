import Link from "next/link";
import { PageIntro } from "@/components/storefront/page-intro";
import { getStorefrontCollections } from "@/lib/storefront-data";
export default async function CollectionsPage() { const collections = await getStorefrontCollections(); return <><PageIntro title="The Collection" description="Thoughtfully designed nightwear that blends comfort with timeless style. Crafted in breathable fabrics with delicate details, each piece is made for the little moments that help you slow down and unwind." /><div className="mx-auto max-w-[1280px] px-6 py-14 md:px-12 md:py-20"><div className="grid gap-4 md:grid-cols-3">{collections.map((collection, index) => <Link key={collection.slug} href={`/shop?collection=${collection.slug}`} className={`${["bg-[#e7d8d0]", "bg-[#d9e0d4]", "bg-[#e7e2d9]"][index % 3]} flex min-h-[360px] flex-col justify-end p-7 transition hover:-translate-y-1`}><h2 className="display text-5xl">{collection.name}</h2><p className="mt-2 text-sm text-ink/60">{collection.description}</p><span className="mt-7 text-[10px] font-bold uppercase tracking-[.16em] underline underline-offset-8">Explore collection</span></Link>)}</div></div></>; }
